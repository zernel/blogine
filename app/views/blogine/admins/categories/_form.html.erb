<%= simple_form_for([:admins, @category], html: { class: 'form-horizontal' }) do |f| %>
  <%= f.input :name %>
  <%= f.input :slug %>

  <%= f.submit class: 'btn btn-primary' %>
  <%= link_to "Back", :back, class: 'btn btn-outline-secondary' %>
<% end %>

